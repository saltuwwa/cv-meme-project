# Qazaq Meme Vision - FastAPI backend
# Install: pip install -r requirements.txt

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
jinja2>=3.0.0

# ML (same as root project)
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
